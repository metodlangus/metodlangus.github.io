const FitTextModule=(()=>{const containerSelector='.quarter-404-page';const overlaySelector='.message-overlay-404-page';const elementSelectors='h1, p, a';const maxFontSizes={h1:500,p:500,a:50};let container,overlay,elements;function fitTextToParent(element,parent){let fontSize=10;element.style.fontSize=fontSize+'px';const parentWidth=parent.clientWidth;const parentHeight=parent.clientHeight;const tag=element.tagName.toLowerCase();const maxLimit=maxFontSizes[tag]||500;while(element.scrollWidth<parentWidth&&element.scrollHeight<parentHeight&&fontSize<maxLimit){fontSize++;element.style.fontSize=fontSize+'px';}
fontSize--;fontSize=Math.floor(fontSize/2);if(fontSize>maxLimit)fontSize=maxLimit;if(tag==='p'&&fontSize<12)fontSize=12;element.style.fontSize=fontSize+'px';}
function resizeAll(){if(!container||!elements.length)return;elements.forEach(el=>fitTextToParent(el,container));}
function main(){container=document.querySelector(containerSelector);if(!container)return;overlay=container.querySelector(overlaySelector);if(!overlay)return;elements=overlay.querySelectorAll(elementSelectors);if(!elements.length)return;resizeAll();window.addEventListener('resize',resizeAll);}
document.addEventListener('DOMContentLoaded',main);return{init:main};})();window.FitTextModule=FitTextModule;